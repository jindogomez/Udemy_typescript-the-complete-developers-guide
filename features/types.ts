const thisMonth = new Date();
thisMonth.getMonth(); // predefined properties and methods of Date

const person = {
    age: 20
};
person.age; // 20

class Color {
    // nothing here
}
const red = new Color(); // has no properties because the class has no members